# Use a generic path in git; keep the actual JSON key file in secrets/ (gitignored).
json_key_file(File.expand_path("../secrets/play-service-account.json", __dir__)) # Path to Play service account JSON (see fastlane supply setup)
package_name("com.androidircx.nulvex") # e.g. com.krausefx.app
